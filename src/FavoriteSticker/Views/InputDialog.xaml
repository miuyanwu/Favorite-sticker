<Window x:Class="FavoriteSticker.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title=""
        Width="320" Height="140"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptTextBlock" Text="Please enter:" FontWeight="SemiBold" Margin="0,0,0,8"/>
        <TextBox x:Name="InputTextBox" Grid.Row="1" Margin="0,0,0,12"
                 VerticalContentAlignment="Center"
                 KeyDown="InputTextBox_KeyDown"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Click="Ok_Click" Padding="16,4" IsDefault="True"
                    Background="{StaticResource AccentBrush}" Foreground="White"
                    BorderThickness="0" Cursor="Hand" Margin="0,0,8,0"/>
            <Button Content="Cancel" Click="Cancel_Click" Padding="16,4" IsCancel="True"
                    BorderThickness="1" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
